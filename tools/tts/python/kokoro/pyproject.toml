[project]
name = "lecture-tts-kokoro"
version = "0.1.0"
description = "Kokoro-82M TTS provider for lecture-automation (CPU-only Intel Mac)"
requires-python = ">=3.10,<3.13"
dependencies = [
    "kokoro-onnx>=0.4.0",
    "misaki[ja]>=0.7.0",
    "numpy>=2.0",
    "soundfile>=0.12",
]
